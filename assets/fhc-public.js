/**
 * Fibonacci Harmony Calculator - front-end behaviour.
 *
 * STUB: populated in Phase 2 (core build). Will mirror the PHP Calculator using
 * BigInt, keep the slider and number box in sync, and live-update every
 * [data-fhc-index] value cell when the seed changes. Container-scoped and
 * class-selector based so multiple instances can coexist on one page.
 */
